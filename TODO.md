1. Add filters, search, pagination, etc to the Clients
2. Fix not being able to add new cases
3. Fix card sizing in personnel
4. Allow CRUD on Marketing campaigns
5. Transactions/Ledger, View Invoice should actually show the invoice and a print icon that is working, also if it's marked as paid then Mark as Paid doesn't show, and remove Send Reminder
6. For Deleting anything, Clients, Cases, Ledger, Marketing, or even Personnel, make it so only the person with CEO/Partner role can do it
7. Implement authentication and authorization using PocketBase + LMS session auth
8. Add the user card in the bottom of the side panel, it should show avatar, full name, and email, also a settings icon and a log out icon, settings modal should have an option to turn on/off 2FA
9. Fix card sizing in Ledger
10. Places where user is able to write Client id or client name, i need you to combine them into a single text box and that text box is supposed to have search and autocomplete support, user can search with name or id in the same text box and backend handles it automatically
11. For side panel, move the dark/light mode into the settings modal